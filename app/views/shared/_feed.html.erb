<% if @feed_items.any? %>
    <ol class="microposts">
      <!-- Lleva :partial, de otra manera no se puede enviar el parametro :collection -->
      <%= render partial: 'shared/feed_item', collection: @feed_items %>
    </ol>
    <%= will_paginate @feed_items %>
<% end %>